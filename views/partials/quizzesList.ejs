<fieldset class="quiz-list">
    <legend><%=title%></legend>
    
    <%for(let quiz of quizzes){%>
      <form class="quiz-list-form">
        <div class="list-link">
          <div>
            <a href="<%='/api/quizzes/' + quiz.id %>"><%=quiz.title%></a>
          </div>
          <div>
            <span class="link-button" disabled>Share with your friends:</span>

            <span ><%='http://loclhost:8080' + '/api/quizzes/' + quiz.id %></span>
          </div>
          
        </div>
        <%if(quiz.user_id == user.id){%>
          <!-- hide edit button as it is not functional for demo -->
          
          <!-- <button class="btn btn-primary quiz-button" type="submit">
            Edit
          </button> -->
          <button formmethod="get" formaction="<%='/api/quizzes/delete/' + quiz.id %>" class="btn btn-primary quiz-button"
            type="submit">
            Delete
          </button>

          

          <%}%>
      </form>
      <%}%>

      

  </fieldset>